# Zabbix server + backing PostgreSQL + frontend web, no namespace `observability`.
# Camada complementar (infra/host + disponibilidade + alertas) — ver ADR-002.
#
# IMPORTANTE: o Secret `zabbix-db` NÃO é versionado (sem credenciais no git — RNF21).
# Crie-o fora do repositório ANTES de aplicar estes manifestos:
#
#   kubectl create namespace observability
#   kubectl -n observability create secret generic zabbix-db \
#     --from-literal=POSTGRES_USER=zabbix \
#     --from-literal=POSTGRES_PASSWORD="$(openssl rand -base64 24)" \
#     --from-literal=POSTGRES_DB=zabbix
#   kubectl apply -f observability/zabbix/zabbix-server.yaml
#
# Em produção, prefira External Secrets Operator / SealedSecrets / Azure Key Vault CSI.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zabbix-postgres
  namespace: observability
  labels:
    app: zabbix-postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zabbix-postgres
  template:
    metadata:
      labels:
        app: zabbix-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          envFrom:
            - secretRef:
                name: zabbix-db
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
      volumes:
        - name: pgdata
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: zabbix-postgres
  namespace: observability
spec:
  selector:
    app: zabbix-postgres
  ports:
    - port: 5432
      targetPort: 5432
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zabbix-server
  namespace: observability
  labels:
    app: zabbix-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zabbix-server
  template:
    metadata:
      labels:
        app: zabbix-server
    spec:
      containers:
        - name: zabbix-server
          image: zabbix/zabbix-server-pgsql:alpine-6.4-latest
          env:
            - name: DB_SERVER_HOST
              value: zabbix-postgres
          envFrom:
            - secretRef:
                name: zabbix-db
          ports:
            - containerPort: 10051
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: zabbix-server
  namespace: observability
spec:
  selector:
    app: zabbix-server
  ports:
    - name: trapper
      port: 10051
      targetPort: 10051
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: zabbix-web
  namespace: observability
  labels:
    app: zabbix-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: zabbix-web
  template:
    metadata:
      labels:
        app: zabbix-web
    spec:
      containers:
        - name: zabbix-web
          image: zabbix/zabbix-web-nginx-pgsql:alpine-6.4-latest
          env:
            - name: DB_SERVER_HOST
              value: zabbix-postgres
            - name: ZBX_SERVER_HOST
              value: zabbix-server
            - name: PHP_TZ
              value: America/Sao_Paulo
          envFrom:
            - secretRef:
                name: zabbix-db
          ports:
            - containerPort: 8080
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: zabbix-web
  namespace: observability
spec:
  type: ClusterIP
  selector:
    app: zabbix-web
  ports:
    - name: http
      port: 80
      targetPort: 8080
