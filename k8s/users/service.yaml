apiVersion: v1
kind: Service
metadata:
  name: hackatonfiap-users
  namespace: conexao-solidaria
  labels:
    app.kubernetes.io/name: hackatonfiap-users
    app.kubernetes.io/part-of: conexao-solidaria
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hackatonfiap-users
  ports:
    - name: http
      port: 8080
      targetPort: http
