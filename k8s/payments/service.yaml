apiVersion: v1
kind: Service
metadata:
  name: hackatonfiap-payments
  namespace: conexao-solidaria
  labels:
    app.kubernetes.io/name: hackatonfiap-payments
    app.kubernetes.io/part-of: conexao-solidaria
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: hackatonfiap-payments
  ports:
    - name: http
      port: 8080
      targetPort: http
